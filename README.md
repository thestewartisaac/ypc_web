
  # Build website with React

  This is a code bundle for Build website with React. The original project is available at https://www.figma.com/design/ACQV3r5HDrKGJRNWaZWTtz/Build-website-with-React.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  